server:
  port: 6666
spring:
  application:
    name: do-ngok
  datasource:
        url: jdbc:mysql://localhost:3306/test-mapping?useUnicode=yes&characterEncoding=UTF-8&useSSL=false&serverTimezone=UTC
        username: root
        password: root
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
        format_sql: false
    show-sql: true